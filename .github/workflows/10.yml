name: WF-Matrix
on: workflow_dispatch
jobs:
   job1:
     runs-on: ${{ matrix.os }}
     strategy:
       matrix:
         os:
         - ubuntu-latest
         - windows-latest
     steps:
     - name: Print Msg 
       run: |
          echo "This is ${{ runner.os }}}"